workflows:
    chrome-workflow:
        name: Chrome Workflow
        max_build_duration: 120
        instance_type: windows_x2
        
        triggering:
            events:                      
                - push

        scripts:
            - name: Update Chrome
              script: | 
                choco install googlechrome --version=146 --pre -y
    
